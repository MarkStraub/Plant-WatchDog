import sun.net.www.protocol.http.BasicAuthentication

// Top-level build file where you can add configuration options common to all sub-projects/modules.
buildscript {
    repositories {
        google()

        maven {
            url = uri("https://maven.pkg.jetbrains.space/data2viz/p/charts-1-r/maven")
//            authentication {
//                create<BasicAuthentication>("basic")
//            }
            credentials {
                username = "XXXXX-XXXXX-XXXXX"         // your client id
                password = "XXXXX-XXXXX-XXXXX"         // your client secret
            }
        }

        mavenCentral()
    }
    dependencies {
        classpath "com.android.tools.build:gradle:7.0.3"
        classpath 'com.google.gms:google-services:4.3.10'
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:1.6.0"

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}